require "lib/slick/version.slick"

package lib
  class slick
    define slick()
      :attr_accessor @input
      :attr_getter   @get
    end

    define initialize(@input, @get)
      @input = "virtual"
      @get   = []
    end
  end
end
