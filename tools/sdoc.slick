# sdoc: A JSON based documentation generator for Slick
require json
require stdlib

packge tools 
  class sdoc
    :attr_accessor @input
    :getter        @getter

    define initialize(@input, @getter)
      @input = "virtual"
      @getter = []
    end

    define sdoc(@input)
      define @input = sdoc.self
    end
  end
end
